## Entry 100 -- 2026-06-10: Replace hard _captureReady gate with 500ms grace window
Files: dist/index.js
- Added 500ms setTimeout fallback to open _captureReady if the restore GET hasn't resolved yet; first lyric status send no longer hard-blocked on the Discord settings fetch round-trip

## Entry 99 -- 2026-06-10: Add User-Agent and X-Discord-Locale to all Discord REST calls
Files: dist/StatusChangerBase.js, dist/index.js
- Added User-Agent (Chrome/136) and X-Discord-Locale (en-US) headers to _discordPatch and index.js token-capture GET; matches what real browser clients send on every request

## Entry 98 -- 2026-06-10: Harden Discord REST + gateway identify payloads
Files: dist/index.js, dist/GatewayClient.js
- index.js token-capture GET now includes X-Super-Properties header (same static value as PATCH calls)
- Gateway identify: fixed os casing (windows -> Windows), added compress:false and intents:0, fixed since:0 -> null when not idle

## Entry 97 -- 2026-06-10: Add static X-Super-Properties to _discordPatch
Files: dist/StatusChangerBase.js
- Added hardcoded X-Super-Properties header (Chrome 136 / build 390018) to all PATCH /users/@me/settings calls; Discord anti-abuse systems flag its absence. Static string — no scraping, no network calls.

## Entry 96 -- 2026-06-10: Remove suspicious Discord net code; upgrade to v10 API
Files: dist/StatusChangerBase.js, dist/index.js
- Removed _fetchBuildNumber IIFE (scraped discord.com/login + asset JS bundles with spoofed UA at startup -- highest-risk item)
- Removed _superProperties (built spoofed X-Super-Properties header for disabled profile color endpoint)
- Removed _discordProfilePatch (only served disabled updateProfileColor)
- Updated _discordPatch + index.js token-capture fetch from discordapp.com/v8 to discord.com/v10
- Set custom_status expires_at to null on all REST sends (was cycling 60s expiry on every send)

## Entry 95 -- 2026-06-10: Fix TDZ crash on startup (_wasPlaying, _pollInterval)
Files: dist/index.js
- Hoisted `let _wasPlaying`, `let _progressInterval`, `let _pollInterval` declarations to before the `if (useDealer)` block; they were declared after the block that used/assigned them, causing temporal dead zone crash on startup.

## Entry 95 -- 2026-06-10: Fix TDZ crash on startup (_wasPlaying, _pollInterval)
Files: dist/index.js
- Hoisted `let _wasPlaying`, `let _progressInterval`, `let _pollInterval` declarations to before the `if (useDealer)` block; they were declared after the block that assigned them, causing "Cannot access before initialization" on startup.

