## Entry 95 -- 2026-06-10: Fix TDZ crash on startup (_wasPlaying, _pollInterval)
Files: dist/index.js
- Hoisted `let _wasPlaying`, `let _progressInterval`, `let _pollInterval` declarations to before the `if (useDealer)` block; they were declared after the block that used/assigned them, causing temporal dead zone crash on startup.

## Entry 95 -- 2026-06-10: Fix TDZ crash on startup (_wasPlaying, _pollInterval)
Files: dist/index.js
- Hoisted `let _wasPlaying`, `let _progressInterval`, `let _pollInterval` declarations to before the `if (useDealer)` block; they were declared after the block that assigned them, causing "Cannot access before initialization" on startup.

